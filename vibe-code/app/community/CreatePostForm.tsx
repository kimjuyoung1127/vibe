// CreatePostForm.tsx
// This component is no longer needed as we've created a community-specific version
// We'll keep it as a placeholder in case we need it later
"use client";

import React from 'react';

const CreatePostForm = () => {
  return null;
};

export default CreatePostForm;